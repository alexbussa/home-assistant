##############################################################
# Sonos Announcements
##############################################################
notify_sonos_kitchen:
  url: !secret notify_sonos_kitchen
notify_sonos_living_room:
  url: !secret notify_sonos_living_room
notify_sonos_rec_room:
  url: !secret notify_sonos_rec_room
notify_sonos_bedroom:
  url: !secret notify_sonos_bedroom
notify_sonos_bathroom:
  url: !secret notify_sonos_bathroom
notify_sonos_den:
  url: !secret notify_sonos_den

##############################################################
# Docker Rest API
##############################################################
docker_restart_sonarr:
  url: !secret docker_restart_sonarr
  method: POST
docker_restart_plex:
  url: !secret docker_restart_plex
  method: POST

##############################################################
# Things Tasks
##############################################################
things_add_task:
  url: !secret things_add_task

##############################################################
# Dashboard Tablets
##############################################################
bedroom_hallway_dashboard_command:
  url: !secret bedroom_hallway_dashboard_command
downstairs_hallway_dashboard_command:
  url: !secret downstairs_hallway_dashboard_command
garage_entrance_dashboard_command:
  url: !secret garage_entrance_dashboard_command
